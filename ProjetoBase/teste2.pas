program varsGlobais (input, output);
var a, b, c: integer;
    k1, temp: integer;
    te: boolean;
begin
    a:=0;
    temp:=10;

    read(a);
    write(10+1,a,temp);

    te := (a > b) and (b = c);

end.